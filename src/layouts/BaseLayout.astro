---
import '../styles/global.css';

interface Theme {
  primaryColor?: string;
  primaryForegroundColor?: string;
  secondaryColor?: string;
  bgColor?: string;
  bgMutedColor?: string;
  bgSecondaryColor?: string;
  textColor?: string;
  textMutedColor?: string;
  fontFamily?: string;
}

interface Props {
  title?: string;
  theme?: Theme;
}

const { title = 'Landing Page Template', theme }: Props = Astro.props;
---

<!doctype html>
<html lang="es">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <title>{title}</title>
    {theme && (
      <style set:html={`:root {
        ${theme.primaryColor ? `--color-primary: ${theme.primaryColor}!important;` : ''}
        ${theme.primaryForegroundColor ? `--color-primary-foreground: ${theme.primaryForegroundColor}!important;` : ''}
        ${theme.secondaryColor ? `--color-secondary: ${theme.secondaryColor}!important;` : ''}
        ${theme.bgColor ? `--color-bg: ${theme.bgColor}!important;` : ''}
        ${theme.bgMutedColor ? `--color-bg-muted: ${theme.bgMutedColor}!important;` : ''}
        ${theme.bgSecondaryColor ? `--color-bg-secondary: ${theme.bgSecondaryColor}!important;` : ''}
        ${theme.textColor ? `--color-text: ${theme.textColor}!important;` : ''}
        ${theme.textMutedColor ? `--color-text-muted: ${theme.textMutedColor}!important;` : ''}
        ${theme.fontFamily ? `--font-sans: '${theme.fontFamily}', system-ui, sans-serif!important;` : ''}
      }`} />
    )}
  </head>
  <body>
    <slot />
  </body>
</html>
