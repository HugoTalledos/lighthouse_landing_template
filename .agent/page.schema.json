{
  "type": "object",
  "additionalProperties": false,
  "required": ["theme", "sections"],
  "properties": {
    "theme": {
      "type": "object",
      "additionalProperties": false,
      "required": ["primary_color", "secondary_color", "font_family", "bg_color", "text_color", "logo_url", "logo_text", "logo_icon"],
      "properties": {
        "primary_color": {
          "type": "string",
          "description": "Hex color, formato ^#[0-9a-fA-F]{3,8}$ (# + 3 a 8 dígitos hex). Un valor inválido rompe el build (astro build lanza un Error nombrando el campo y el valor), no hay fallback silencioso."
        },
        "secondary_color": {
          "type": "string",
          "description": "Mismo formato hex que primary_color (^#[0-9a-fA-F]{3,8}$). Mismo comportamiento de fallo duro ante un valor inválido."
        },
        "font_family": {
          "type": "string",
          "description": "Formato ^[A-Za-z0-9 _-]+$ (letras, dígitos, espacios, _ y - solamente; sin comas ni comillas). Un solo nombre de fuente (ej. \"Poppins\"), no un font-stack CSS. Valor inválido rompe el build igual que los colores."
        },
        "bg_color": {
          "type": ["string"],
          "description": "Mismo formato hex que primary_color (^#[0-9a-fA-F]{3,8}$) cuando no es null — mismo fallo duro ante un valor inválido. Sobreescribe --color-bg (fondo del body). Omitir o null cae al default de global.css (#ffffff)."
        },
        "text_color": {
          "type": ["string"],
          "description": "Mismo formato hex que primary_color (^#[0-9a-fA-F]{3,8}$) cuando no es null — mismo fallo duro ante un valor inválido. Sobreescribe --color-text (texto principal del body). Omitir o null cae al default de global.css (#111827)."
        },
        "logo_url": {
          "type": ["string", "null"],
          "description": "Opcional. Interpolado sin sanitizar en <img src>. Se renderiza como el logo del header solo si logo_icon está ausente/null — si logo_icon también está seteado, logo_icon gana y logo_url se ignora. Omitir o null para no usar imagen."
        },
        "logo_text": {
          "type": ["string", "null"],
          "description": "Opcional. Pasa directo a la prop logoText de Hero sin validación/sanitización. Omitir o null cae al default del átomo Logo ('[Tu Marca]')."
        },
        "logo_icon": {
          "type": ["string", "null"],
          "description": "Opcional. SVG crudo, se inyecta sin sanitizar vía set:html. Mismo modelo de confianza que HeroSection.image.url y FeatureItem.icon — solo usar SVG confiable/sanitizado. Omitir o null para no usar ícono."
        }
      }
    },
    "sections": {
      "type": "array",
      "minItems": 1,
      "description": "Array de secciones discriminadas por 'type'. index.astro busca a lo sumo una sección por type vía .find() — si hay dos objetos con el mismo 'type', solo se usa la primera. El orden en el array no afecta el orden de render (fijo: Hero → Beneficios → Pricing → Testimonials → FAQ → CTA → CaptureForm → Footer), solo decide cuál gana en caso de type duplicado. Un 'type' no listado en $defs es ignorado silenciosamente, sin error.",
      "items": {
        "anyOf": [
          { "$ref": "#/$defs/HeroSection" },
          { "$ref": "#/$defs/FeaturesSection" },
          { "$ref": "#/$defs/CaptureSection" },
          { "$ref": "#/$defs/PricingSection" },
          { "$ref": "#/$defs/TestimonialsSection" },
          { "$ref": "#/$defs/FaqSection" },
          { "$ref": "#/$defs/CtaSection" },
          { "$ref": "#/$defs/FooterSection" }
        ]
      }
    }
  },
  "$defs": {
    "HeroSection": {
      "type": "object",
      "additionalProperties": false,
      "description": "Renderiza incondicionalmente. Si la sección 'hero' está ausente del array, index.astro cae a copy placeholder genérica en español — para una página real, siempre inclúyela. El CTA del hero siempre enlaza a #formulario-captura (ancla de CaptureForm); no es configurable desde este JSON. Las llaves están agrupadas por función (text/image), reflejando el agrupamiento de props del componente Hero.",
      "required": ["type", "text", "image"],
      "properties": {
        "type": { "const": "hero" },
        "layout": {
          "type": ["string", "null"],
          "enum": ["side", "background", null],
          "description": "Opcional. Mapea a la prop layout de Hero. 'side' (default si se omite/null) muestra image.url al costado del texto, como hoy. 'background' la usa como fondo de toda la sección con un overlay oscuro semitransparente para contraste, y el título/subtítulo pasan a texto claro. Si es 'background' pero image.url es null, cae al layout 'side' (placeholder incluido)."
        },
        "text": { "$ref": "#/$defs/HeroText" },
        "image": { "$ref": "#/$defs/HeroImage" }
      }
    },
    "HeroText": {
      "type": "object",
      "additionalProperties": false,
      "required": ["headline", "subheadline", "cta_text"],
      "properties": {
        "headline": { "type": "string", "description": "Mapea a la prop title de Hero." },
        "subheadline": { "type": "string", "description": "Mapea a la prop subtitle de Hero." },
        "cta_text": { "type": "string", "description": "Mapea a la prop ctaLabel de Hero." }
      }
    },
    "HeroImage": {
      "type": "object",
      "additionalProperties": false,
      "required": ["url"],
      "properties": {
        "url": {
          "type": ["string", "null"],
          "description": "Opcional. Mapea a img.imageUrl de Hero. Interpolado en <img src> sin sanitizar. Si se omite/null, renderiza el placeholder '[Espacio para imagen o ilustración]' (aplica sin importar el valor de layout)."
        },
        "position": {
          "type": ["string", "null"],
          "enum": ["left", "right", null],
          "description": "Opcional. Mapea a img.sideConfig.position de Hero. Solo aplica con layout 'side' (o su fallback desde 'background' sin url). Indica el lado donde va la imagen (o su placeholder); el texto se ubica del lado contrario. Default 'right' si se omite/null."
        },
        "shape": {
          "type": ["string", "null"],
          "enum": ["circle", "rounded", null],
          "description": "Opcional. Mapea a img.sideConfig.shape de Hero. Solo aplica con layout 'side' (o su fallback). 'rounded' (default si se omite/null) usa esquinas redondeadas; 'circle' usa un radius completo."
        },
        "blur": {
          "type": ["boolean", "null"],
          "description": "Opcional. Mapea a img.backgroudConfig.blur de Hero. Solo aplica con layout 'background'. Default false si se omite/null."
        },
        "overlay_opacity": {
          "type": ["number", "null"],
          "minimum": 0,
          "maximum": 1,
          "description": "Opcional. Mapea a img.backgroudConfig.overlayOpacity de Hero. Solo aplica con layout 'background'. Controla la opacidad del overlay oscuro sobre la imagen de fondo (0 = sin overlay, 1 = negro sólido). Default 0.55 si se omite/null."
        }
      }
    },
    "FeaturesSection": {
      "type": "object",
      "additionalProperties": false,
      "description": "Renderiza incondicionalmente. Si la sección 'features' está ausente, cae a copy placeholder genérica — para una página real, siempre inclúyela.",
      "required": ["type", "headline", "items"],
      "properties": {
        "type": { "const": "features" },
        "headline": {
          "type": ["string", "null"],
          "description": "Opcional. Mapea a la prop heading de BenefitsSection. Falls back a 'Beneficios' si se omite/null."
        },
        "items": {
          "type": "array",
          "description": "Mapea a la prop items de BenefitsSection. Longitud no fija a 3 — el grid ajusta para otras cantidades.",
          "items": { "$ref": "#/$defs/FeatureItem" }
        }
      }
    },
    "FeatureItem": {
      "type": "object",
      "additionalProperties": false,
      "required": ["title", "description", "icon"],
      "properties": {
        "title": { "type": "string" },
        "description": { "type": "string" },
        "icon": {
          "type": ["string", "null"],
          "description": "Opcional. Un string que empieza con '<' se trata como SVG inline crudo e inyecta sin sanitizar vía set:html; cualquier otro string no vacío (ej. \"check\", un emoji como \"🚚\") se renderiza como texto plano. Omitir o null cae al ícono checkmark placeholder. Preferir un emoji plano sobre SVG a mano salvo que ya se tenga un SVG confiable y sanitizado."
        }
      }
    },
    "CaptureSection": {
      "type": "object",
      "additionalProperties": false,
      "description": "Siempre renderiza (como hero/features), pero a diferencia de esas dos, omitir la sección 'capture' entera también está bien: index.astro cae al copy default campo por campo. Los tres campos son individualmente opcionales — se puede setear solo uno y confiar en los defaults para el resto. Esta sección solo controla copy; los campos del formulario (nombre/email/celular) y su validación están hardcodeados en CaptureForm.astro/CaptureForm.client.ts.",
      "required": ["type", "headline", "subheadline", "cta_text"],
      "properties": {
        "type": { "const": "capture" },
        "headline": {
          "type": ["string", "null"],
          "description": "Opcional. Mapea a heading de CaptureForm. Falls back a 'Déjanos tus datos' si se omite/null o si la sección entera está ausente."
        },
        "subheadline": {
          "type": ["string", "null"],
          "description": "Opcional. Mapea a subtitle de CaptureForm. Falls back a 'Te contactaremos con más información.'."
        },
        "cta_text": {
          "type": ["string", "null"],
          "description": "Opcional. Mapea a ctaLabel de CaptureForm. Falls back a 'Enviar'."
        }
      }
    },
    "PricingSection": {
      "type": "object",
      "additionalProperties": false,
      "description": "Renderiza condicionalmente: solo si está presente en 'sections'. No hay copy/fallback placeholder — omitir la sección entera si la página no debe tener pricing, no incluir una vacía/parcial.",
      "required": ["type", "headline", "plans"],
      "properties": {
        "type": { "const": "pricing" },
        "headline": {
          "type": ["string", "null"],
          "description": "Opcional. Mapea a heading de PricingSection. Falls back a 'Planes' si se omite/null."
        },
        "plans": {
          "type": "array",
          "description": "Grid ajusta para cantidades distintas de 3.",
          "items": { "$ref": "#/$defs/PricingPlan" }
        }
      }
    },
    "PricingPlan": {
      "type": "object",
      "additionalProperties": false,
      "description": "No existe un cta_url por plan — el botón de cada PricingCard siempre enlaza al ancla default #formulario-captura; la plantilla no tiene campo para sobreescribirlo por plan.",
      "required": ["name", "price", "period", "features", "cta_text"],
      "properties": {
        "name": { "type": "string", "description": "Mapea a la prop name de PricingCard." },
        "price": {
          "type": "string",
          "description": "Mapea a la prop price de PricingCard. String libre, no un número — incluir símbolo de moneda, ej. \"$15.99\"."
        },
        "period": {
          "type": ["string", "null"],
          "description": "Opcional. Mapea a period de PricingCard. Se añade en tono apagado tras el precio, ej. '/mes'. Omitir o null para un precio único sin periodo."
        },
        "features": {
          "type": "array",
          "description": "Mapea a features de PricingCard. Se renderiza como lista con prefijo ✓.",
          "items": { "type": "string" }
        },
        "cta_text": { "type": "string", "description": "Mapea a ctaLabel de PricingCard." }
      }
    },
    "TestimonialsSection": {
      "type": "object",
      "additionalProperties": false,
      "description": "Renderiza condicionalmente: solo si está presente en 'sections'. No hay copy/fallback placeholder.",
      "required": ["type", "headline", "items"],
      "properties": {
        "type": { "const": "testimonials" },
        "headline": {
          "type": ["string", "null"],
          "description": "Opcional. Mapea a heading de TestimonialsSection. Falls back a 'Lo que dicen nuestros clientes' si se omite/null."
        },
        "items": {
          "type": "array",
          "description": "Grid md:grid-cols-2.",
          "items": { "$ref": "#/$defs/TestimonialItem" }
        }
      }
    },
    "TestimonialItem": {
      "type": "object",
      "additionalProperties": false,
      "required": ["quote", "author_name", "author_role"],
      "properties": {
        "quote": {
          "type": "string",
          "description": "Mapea a quote de TestimonialCard. Se renderiza envuelto en comillas curvas; no añadir comillas propias."
        },
        "author_name": { "type": "string", "description": "Mapea a authorName de TestimonialCard." },
        "author_role": {
          "type": ["string", "null"],
          "description": "Opcional. Mapea a authorRole de TestimonialCard. Se omite por completo del render si está ausente/null (ej. para un testimonio anónimo)."
        }
      }
    },
    "FaqSection": {
      "type": "object",
      "additionalProperties": false,
      "description": "Renderiza condicionalmente: solo si está presente en 'sections'. No hay copy/fallback placeholder.",
      "required": ["type", "headline", "items"],
      "properties": {
        "type": { "const": "faq" },
        "headline": {
          "type": ["string", "null"],
          "description": "Opcional. Mapea a heading de FaqSection. Falls back a 'Preguntas frecuentes' si se omite/null."
        },
        "items": {
          "type": "array",
          "description": "Se apila de arriba hacia abajo (no es un grid).",
          "items": { "$ref": "#/$defs/FaqEntry" }
        }
      }
    },
    "FaqEntry": {
      "type": "object",
      "additionalProperties": false,
      "required": ["question", "answer"],
      "properties": {
        "question": { "type": "string", "description": "Mapea a question de FaqItem." },
        "answer": { "type": "string", "description": "Mapea a answer de FaqItem." }
      }
    },
    "CtaSection": {
      "type": "object",
      "additionalProperties": false,
      "description": "Renderiza condicionalmente: solo si está presente en 'sections'. A diferencia de pricing/testimonials/faq, el copy de esta sección SÍ se lee completo del JSON (sin fallback hardcodeado) — es un banner de CTA independiente, distinto del hero.",
      "required": ["type", "headline", "subheadline", "button_text", "button_url"],
      "properties": {
        "type": { "const": "cta" },
        "headline": { "type": "string", "description": "Mapea a headline de CtaSection." },
        "subheadline": {
          "type": ["string", "null"],
          "description": "Opcional. Mapea a subheadline de CtaSection. Se omite por completo del render si está ausente/null."
        },
        "button_text": { "type": "string", "description": "Mapea a buttonLabel de CtaSection." },
        "button_url": {
          "type": ["string", "null"],
          "description": "Opcional. Mapea a buttonHref de CtaSection. Default #formulario-captura si se omite/null."
        }
      }
    },
    "FooterSection": {
      "type": "object",
      "additionalProperties": false,
      "description": "Renderiza condicionalmente: solo si está presente en 'sections'. No hay copy/fallback placeholder.",
      "required": ["type", "business_name", "links", "social_links"],
      "properties": {
        "type": { "const": "footer" },
        "business_name": {
          "type": "string",
          "description": "Mapea a businessName de Footer. Se renderiza como texto plano en negrita, no a través del componente Logo — puede diferir de theme.logo_text."
        },
        "links": {
          "type": "array",
          "description": "Mapea a links de Footer.",
          "items": { "$ref": "#/$defs/FooterLink" }
        },
        "social_links": {
          "type": ["array", "null"],
          "description": "Opcional. Mapea a socialLinks de Footer. Omitir o dejar vacío/null oculta el nav social por completo. 'platform' se renderiza como texto plano (ej. \"Instagram\") — no hay íconos por plataforma en esta plantilla, no esperar que aparezca un ícono sin importar el valor usado.",
          "items": { "$ref": "#/$defs/SocialLink" }
        }
      }
    },
    "FooterLink": {
      "type": "object",
      "additionalProperties": false,
      "required": ["label", "url"],
      "properties": {
        "label": { "type": "string" },
        "url": { "type": "string" }
      }
    },
    "SocialLink": {
      "type": "object",
      "additionalProperties": false,
      "required": ["platform", "url"],
      "properties": {
        "platform": { "type": "string" },
        "url": { "type": "string" }
      }
    }
  }
}
